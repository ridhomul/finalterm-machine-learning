# The provided datasets are larger than GitHub's normal file-size limit.
# Track CSV files with Git LFS if your lecturer requires datasets in the repository.
*.csv filter=lfs diff=lfs merge=lfs -text
